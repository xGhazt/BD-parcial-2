/**
 * TODO: controladores de ordenes
 * - createOrder (from cart), getOrders, getOrdersByUser, updateOrderStatus
 */
export const createOrder = async (req, res, next) => { res.status(501).json({ error: "Not implemented" }); };
